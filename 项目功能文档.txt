项目设计文档
一、 项目概述
项目名称：智能组卷系统
二、 技术栈说明
前端：Vue.js（推荐使用Vue 3，配合Vite构建工具；UI库可选Element Plus）。
后端：Python Flask（轻量级Web框架，用于提供RESTful API）。
数据库：MySQL（用于存储用户、题库、试卷、教材等所有结构化数据）。
核心AI服务：集成大模型API用于题目生成与智能校验功能。
三、 系统架构设计
前后端分离架构：
前端Vue应用独立部署，通过HTTP请求与后端交互。
后端Flask应用提供纯数据接口，负责业务逻辑、数据存取及AI服务集成。
前后端通过JSON格式进行数据交换。
核心模块划分：
1. 题库管理模块
职责：负责题目的全生命周期管理，是系统的数据核心。
关键流程：
题目入库：支持用户手动录入或通过Excel/Word模板批量上传题目。上传后解析题目文本、选项、答案、解析，并等待用户或AI为其标注难度、章节、题型。
题目检索：为组卷模块提供高效的查询能力，能根据多重筛选条件（章节、难度、题型、知识点）组合查询题目。
题库维护：提供题目的简单增、删、改、查界面，并记录题目的最后修改时间和状态。
2. AI出题模块
职责：负责让AI出题
关键设计：
上下文构建：根据用户选择的教材章节，提取章节概要和已有题目，构建出题上下文。
大模型调用：封装调用大模型API的逻辑，传入上下文和出题要求，获取生成的题目。
题目存储：将生成的题目存入题库，标记为“待校验”状态，等待后续的质量审核。
还可以根据需要，设计题目生成的参数，如题型、难度、数量等。
还可以根据一张已存在的试卷，生成类似风格的试卷题目。
3. 智能题库更新模块
职责：利用大模型扩展题库。
关键流程：
1.用户选择目标教材、章节，并设定期望的题型、难度和生成数量。
2.后端将目标章节的“章节概要”、已有的相关题目作为上下文，调用大模型API，生成新题目。
3.生成的新题目首先存入临时表，状态标记为“待校验”，不会直接进入主题库。
4. 题目校验模块
职责：确保题目质量，是“智能更新”流程的质量关卡。
关键流程：
用户手动校验：用户界面集中展示所有“待校验”题目及AI的校验建议。用户可以：
一键通过
修改题目内容或分类，然后通过。
直接拒绝不合格题目。
只有用户确认通过的题目，才会设置成校验通过。
5. 智能组卷模块
职责：根据策略，从题库中自动筛选题目组成试卷。
关键设计：
组卷策略：由用户在前端设置，是一个包含信息的JSON格式的对象
组卷算法：根据策略，将其转化为对题库的查询条件，进行多轮随机筛选，直至满足所有分布要求。核心原则是只从现有题库中抽取，不动态生成新题。
6. 试卷编辑与导出模块
职责：处理试卷的最终呈现与输出。
关键流程：
预览与编辑：组卷生成的题目列表，在前端渲染为可预览的试卷样式。用户可调整题目顺序、修改格式、添加页眉页脚。
导出：后端根据编辑后的试卷数据，使用模板引擎生成Word文档，或转换为PDF文件供下载。
历史存档：每次导出的试卷版本（包含所有编辑内容）会完整保存，便于回溯。
6.教材上传与管理模块
职责：管理教材资源，支持用户上传和选择教材。
关键设计：
教材上传：用户可上传教材文件（PDF、Word等格式），系统解析并提取章节信息。也可以让用户上传Excel文件，批量录入章节信息。
章节概要生成：调用大模型API，为每个章节生成简要概要，便于后续出题和组卷使用。
教材管理界面：提供教材列表、章节浏览和删除功能。
7.数据库采用Mysql本地连接，数据库名是exam_system-v3，用户名是root，用户密码是201225.数据库的十一张表已经创建，并且不需要修改表的结构。
8.AI采用的是deepseek模型，因为是测试阶段所以API秘钥显式存储。

